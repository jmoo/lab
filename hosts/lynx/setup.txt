### First time setup

1. Run `sudo nixos-rebuild switch --flake github:jmoo/lab#lynx`
2. Run `gpg --full-generate-key`
3. Run `ssh-keygen`
4. Add ssh pubkey to github
5. Run `mkdir -p /home/jmoore/Repos/jmoo/lab && git clone git@github.com:jmoo/lab /home/jmoore/Repos/jmoo/lab`
6. Run `switch`